name: lean_builder
description: A streamlined Dart build system that applies lean principles to minimize waste and maximize speed. Designed for developers who value efficiency and performance.
version: 0.1.1
repository: https://github.com/cuongtc2010/lean_builder
platforms:
  macos:
  linux:
  windows:

topics:
  - build
  - build-system
  - dart-build-system
  - code-generation
  - codegen

executables:
  lean_builder:

environment:
  sdk: ^3.8.0

# Add regular dependencies here.
dependencies:
  _fe_analyzer_shared: ^89.0.0
  analyzer: ^8.2.0
  args: ^2.7.0
  xxh3: ^1.2.0
  path: ^1.9.1
  collection: ^1.18.0
  meta: 1.16.0
  glob: ^2.1.3
  watcher: ^1.1.2
  dart_style: ^3.0.0
  ansicolor: ^2.0.3
  source_span: ^1.10.1
  stack_trace: ^1.12.1
  frontend_server_client: ^4.0.0
  yaml: ^3.1.3
  lints: ^6.0.0
  test: ^1.25.15
  hotreloader: ^4.3.0
